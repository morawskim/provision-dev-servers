gitlab_runners:
    mmo:
        tokens:
            docker: 'DOCKER_TOKEN'
            shell: 'SHELL_TOKEN'
            gitlab: 'GITLAB_TOKEN'
            gitlab-docker: 'GITLAB_DOCKER_TOKEN'

backup:
  password: 'HASLO!!!'

mailgun::username: 'USERNAME'
mailgun::password: 'PASSWORD'

workstation::mailgun::username: 'USERNAME'
workstation::mailgun::password: 'PASSWORD'
workstation::mailgun::admin_addr: 'email@domain.com'

docker_registries:
  docker_hub:
    server: https://index.docker.io/v1/
    ensure: present
    username: test
    password: securepassword
    email: test@example.com
    local_user: vagrant
  ssorder:
    server: https://registry.gitlab.com
    ensure: present
    username: username
    password: token
    local_user: vagrant
