apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: euro-office
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`office.morawskim.pl`)
      services:
        - kind: Service
          name: euro-office
          port: 80
      middlewares:
      #   - name: openid-auth
        - name: add-x-forwarded-proto
  tls:
    certResolver: myresolver
