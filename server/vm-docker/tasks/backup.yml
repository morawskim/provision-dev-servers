- name: Install backup software
  package:
    name:
      - ruby
      - cron
      - duplicity
      - python3-packaging
      - python3-boto3
      - awscli
- name: Creates backups directory
  file:
    path: "{{ BACKUP_DIR }}"
    state: directory
    owner: root
    group: root
    mode: '0750'
- name: Copy script to dump mysql databases
  copy:
    src: backup/dump-mysql.rb
    dest: /usr/local/bin/dump-mysql.rb
    owner: root
    group: root
    mode: '0755'
- name: Copy script to dump nextcloud
  copy:
    src: backup/nextcloud-backup.sh.j2
    dest: /usr/local/bin/nextcloud-backup.sh
    owner: root
    group: root
    mode: '0755'
- name: Copy script to backup
  copy:
    src: backup/backup.sh.j2
    dest: /usr/local/bin/backup.sh
    owner: root
    group: root
    mode: '0755'
- name: Copy duplicity AWS wrapper
  copy:
    src: backup/duplicity-aws-assume-role.sh
    dest: /usr/local/bin/duplicity-aws-assume-role.sh
    owner: root
    group: root
    mode: '0700'
- name: Creates a cron job for backup
  cron:
    name: "daily backup"
    special_time: daily
    user: root
    job: "/usr/local/bin/backup.sh {{ BACKUP_DIR }}"
    cron_file: backup
# {{{ Duplicity
- name: Create env file for duplicity
  no_log: true
  template:
    src: backup/duplicity.env.j2
    dest: "/etc/default/duplicity.env"
    owner: root
    group: root
    mode: '0640'
- name: Create service's systemd unit
  template:
    src: backup/duplicity.service.j2
    dest: "/etc/systemd/system/duplicity.service"
- name: Create timer's systemd unit
  template:
    src: backup/duplicity.timer.j2
    dest: "/etc/systemd/system/duplicity.timer"
- name: Reload systemd
  systemd:
    daemon_reload: yes
- name: Enable a service unit for duplicity
  systemd:
    name: duplicity.service
    enabled: yes
    state: stopped
- name: Enable a timer unit for duplicity
  systemd:
    name: duplicity.timer
    state: started
    enabled: yes
- name: Add backup gpg key
  gpg_key:
    content: "{{ lookup('file', 'backup/0x1E94F1FB.asc') }}"
    trust: '5'
# }}}
